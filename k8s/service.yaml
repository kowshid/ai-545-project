apiVersion: v1
kind: Service
metadata:
  name: insurance-app-service
  labels:
    app: insurance-app
spec:
  type: NodePort
  selector:
    app: insurance-app
  ports:
    - name: http
      port: 8501
      targetPort: 8501
      nodePort: 30085
